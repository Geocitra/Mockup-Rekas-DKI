services:
  web:
    image: nginx:alpine
    container_name: rekas-demo-web
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html
    networks:
      - e-daily-net

networks:
  e-daily-net:
    external: true
    name: e-daily-report_default

